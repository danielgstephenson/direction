const s = 0.17

export const oneLine = [
  -0.3 * s, +s,
  0, +s,
  0, -s
]

export const twoLine = [
  -s, +s,
  +s, +s,
  +s, +0,
  -s, +0,
  -s, -s,
  +s, -s
]

export const threeLine = [
  -s, +s,
  +s, +s,
  +s, +0,
  -0.5 * s, +0,
  +s, +0,
  +s, -s,
  -s, -s
]

export const fourLine = [
  -s, +s,
  -s, +0,
  +s, +0,
  +s, +s,
  +s, -s
]

export const fiveLine = [
  +s, +s,
  -s, +s,
  -s, +0,
  +s, +0,
  +s, -s,
  -s, -s
]

export const sixLine = [
  +s, +s,
  -s, +s,
  -s, +0,
  +s, +0,
  +s, -s,
  -s, -s,
  -s, +0
]
